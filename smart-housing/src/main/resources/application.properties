spring.application.name=smart-housing
#Database connection
spring.datasource.url=jdbc:mysql://localhost:3306/smart_housing
spring.datasource.username=root
spring.datasource.password=42131607k

# JPA settings
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

#Handling the multi-part data
# =======================================================
# MULTIPART CONFIGURATION (Crucial for handling files)
# =======================================================

# Enables multipart support
spring.servlet.multipart.enabled=true

# Location where files will be temporarily stored
spring.servlet.multipart.location=

# Max file size (e.g., 10MB)
spring.servlet.multipart.max-file-size=10MB

# Max request size (sum of all files + form data, e.g., 10MB)
spring.servlet.multipart.max-request-size=10MB